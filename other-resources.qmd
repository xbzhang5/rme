- @dunn2018generalized is a recent textbook on GLMs. It doesn't cover time-to-event models, and it doesn't use the modern `tidyverse` packages (`ggplot2`, `dplyr`, etc.), but otherwise it seems great.
